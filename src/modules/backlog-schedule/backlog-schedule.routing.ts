import { Routes } from "@angular/router";
import { BacklogScheduleComponent } from "./detail/backlog-schedule.component";

const routes: Routes = [
    {
        path: "",
        component: BacklogScheduleComponent
    }
];

export default routes;
